//Sum of Natural Number Using Recurssion//
#include<stdio.h>
int sum(int n)
{
	if(n==0)
	return 0;
	else
	return n+sum(n-1);
	}
	int main()
	{
	int n;
	printf("enter limit:");
	scanf("%d",&n);
	printf("\n sum of natural numbers=%d",sum(n));
	}
